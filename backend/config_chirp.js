//Telling the database the application's name,
//the name of the database, and where to connect
//with the frontend of the application.

export const appName = 'REACTionary_Canary';
export const port = 3000;
export const db = {
  host: 'localhost:3000',
  dbName: 'canary_compliments'
};